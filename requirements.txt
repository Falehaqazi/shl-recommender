# Web stack
fastapi==0.115.0
uvicorn[standard]==0.30.6
pydantic==2.9.2

# LangGraph + agent
langgraph==0.2.34

# LLM providers
groq==0.11.0
google-generativeai==0.8.3

# Retrieval
rank-bm25==0.2.2
sentence-transformers==3.1.1
# sentence-transformers pulls torch transitively. We don't pin torch
# explicitly so Render can pick a CPU wheel matching the base image.
numpy>=1.26,<2.1

# Scraper backup
httpx==0.27.2
beautifulsoup4==4.12.3

# Dev / eval
pytest==8.3.3
